apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    app: crypto-server-service
  name: crypto-server-service
spec:
  ports:
  - name: 5000-5000
    port: 5000
    protocol: TCP
    targetPort: 5000
  selector:
    app: crypto-server-service
  type: ClusterIP
status:
  loadBalancer: {}
